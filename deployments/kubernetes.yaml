apiVersion: v1
kind: Namespace
metadata:
  name: platform
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: swagger-service
  namespace: platform
automountServiceAccountToken: true
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: swagger-service-discovery
rules:
  - apiGroups: [""]
    resources: [services]
    verbs: [get, list, watch]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: swagger-service-discovery
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: swagger-service-discovery
subjects:
  - kind: ServiceAccount
    name: swagger-service
    namespace: platform
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: swagger-service
  namespace: platform
data:
  APP_ENV: production
  APP_HTTP_ADDRESS: 0.0.0.0:8080
  APP_GRPC_ADDRESS: 0.0.0.0:9090
  APP_GRPC_ENABLED: "false"
  APP_OBSERVABILITY_METRICS_ENABLED: "true"
  APP_AGGREGATION_KUBERNETES_ENABLED: "true"
  APP_AGGREGATION_KUBERNETES_NAMESPACE: ""
  APP_AGGREGATION_KUBERNETES_LABEL_SELECTOR: platform.swagger/enabled=true
  APP_AUTH_JWKS_URL: http://identity-service.platform.svc:8080/.well-known/jwks.json
  APP_AUTH_ISSUER: identity-service
  APP_AUTH_AUDIENCE: swagger-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: swagger-service
  namespace: platform
  labels:
    app.kubernetes.io/name: swagger-service
spec:
  replicas: 2
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: swagger-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: swagger-service
    spec:
      serviceAccountName: swagger-service
      automountServiceAccountToken: true
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: swagger-service
      containers:
        - name: api
          image: ghcr.io/lihongjie0209/swagger-service:main
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: swagger-service
            - secretRef:
                name: swagger-service
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: grpc
              containerPort: 9090
              protocol: TCP
          startupProbe:
            httpGet:
              path: /live
              port: http
            periodSeconds: 2
            timeoutSeconds: 2
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /live
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: [sh, -c, sleep 5]
          volumeMounts:
            - name: logs
              mountPath: /app/logs
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            readOnlyRootFilesystem: true
      volumes:
        - name: logs
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: swagger-service
  namespace: platform
  labels:
    app.kubernetes.io/name: swagger-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: swagger-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
    - name: grpc
      port: 9090
      targetPort: grpc
      protocol: TCP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: swagger-service
  namespace: platform
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: swagger-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: swagger-service
  namespace: platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: swagger-service
  minReplicas: 2
  maxReplicas: 10
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: swagger-service
  namespace: platform
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: swagger-service
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector: {}
      ports:
        - {port: 8080, protocol: TCP}
        - {port: 9090, protocol: TCP}
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - {port: 53, protocol: UDP}
        - {port: 53, protocol: TCP}
    - ports:
        - {port: 3306, protocol: TCP}
        - {port: 5432, protocol: TCP}
        - {port: 6379, protocol: TCP}
        - {port: 4318, protocol: TCP}
        - {port: 8080, protocol: TCP}
        - {port: 443, protocol: TCP}
